# Reusable workflow: run pyqtest's own checks against a consumer repo.
#
# Usage from a consumer repo:
#
#   .github/workflows/pyqtest.yml:
#     jobs:
#       pyqtest:
#         uses: acoustesh/pyqtest/.github/workflows/pyqtest.yml@v1
#         with:
#           mode: ratchet    # or "strict"
#           python-version: "3.12"
#
# Workflow_call inputs (all optional except mode):
name: pyqtest

on:
  workflow_call:
    inputs:
      python-version:
        description: "Python version"
        required: false
        type: string
        default: "3.12"
      mode:
        description: "pyqtest mode (ratchet or strict)"
        required: true
        type: string
      install-local:
        description: "Install pyqtest-local for similarity"
        required: false
        type: boolean
        default: false
      install-cloud:
        description: "Install pyqtest-cloud for similarity"
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  pyqtest:
    name: pyqtest (${{ inputs.mode }})
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Install pyqtest from source
        run: |
          python -m pip install --upgrade pip
          python -m pip install .
          if [ "${{ inputs.install-local }}" = "true" ]; then
            pip install ./pyqtest-local
          fi
          if [ "${{ inputs.install-cloud }}" = "true" ]; then
            pip install ./pyqtest-cloud
          fi

      - name: Init baselines (first run only)
        run: |
          if [ ! -f tests/baselines/lint_typecheck.json ]; then
            pytest --update-baselines || true
          fi

      - name: Run pyqtest (${{ inputs.mode }})
        run: pytest -m "not experimental" --tb=short

      - name: Ratchet baselines (ratchet mode only)
        if: inputs.mode == 'ratchet'
        run: |
          pyqtest ratchet || true
          git diff --exit-code tests/baselines/ || {
            echo "::warning::Baselines were ratcheted — review the diff and commit."
            exit 0
          }