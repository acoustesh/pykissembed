"""Similarity check stub.

The full similarity module is provided by ``pyqtest[local]`` or
``pyqtest-cloud`` (sentence-transformers / OpenAI / Voyage / Codestral /
Gemini providers). Without one of those extras installed, similarity
checks are skipped with a friendly message.
"""
